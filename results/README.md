# Results Directory

This folder contains generated experiment outputs (images, metrics JSON, progress grids).

Results are excluded from Git by default.
Keep only small curated figures in `figures/` for publication.
