#!/bin/bash
set -euo pipefail

# Road-conditioned pipeline is experimental and currently not fully wired.
# See README section "Experimental components" before using this script.

echo "run_cond_road.sh is disabled: the road-conditioned pipeline needs a custom dataset loader and script cleanup."
exit 1
